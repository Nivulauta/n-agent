{
    "name": "websocket-connect",
    "version": "1.0.0",
    "description": "WebSocket connection handler Lambda",
    "main": "dist/index.mjs",
    "scripts": {
        "build": "node build.mjs",
        "compile": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.600.0",
        "@aws-sdk/lib-dynamodb": "^3.600.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.161",
        "@types/node": "^20.19.35",
        "aws-sdk-client-mock": "^4.0.0",
        "fast-check": "^3.15.0",
        "typescript": "^5.4.5",
        "vitest": "^2.1.9"
    },
    "type": "module"
}
