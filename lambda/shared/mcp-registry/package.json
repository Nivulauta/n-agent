{
    "name": "mcp-registry",
    "version": "1.0.0",
    "description": "MCP Tool Registry — CRUD for MCP server configurations in DynamoDB",
    "main": "dist/index.mjs",
    "scripts": {
        "build": "node build.mjs",
        "build:tsc": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest --run"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.600.0",
        "@aws-sdk/lib-dynamodb": "^3.600.0"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "typescript": "^5.4.5",
        "vitest": "^1.6.0"
    },
    "type": "module"
}