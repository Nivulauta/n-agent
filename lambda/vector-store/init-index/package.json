{
    "name": "init-index",
    "version": "1.0.0",
    "description": "Lambda function to initialize OpenSearch index with k-NN configuration",
    "type": "module",
    "main": "dist/index.mjs",
    "scripts": {
        "build": "node build.mjs",
        "test": "vitest run",
        "test:watch": "vitest",
        "clean": "rm -rf dist node_modules"
    },
    "dependencies": {
        "@opensearch-project/opensearch": "^2.12.0",
        "@aws-sdk/credential-provider-node": "^3.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3",
        "vitest": "^1.0.4"
    }
}